<div class="formContiner">
    <div class="changePassword">
        <h2 class="h2title" >Cambiar Contraseña</h2>
<form class="changeForm" action="/api/sessions/change_password" method="POST">
<label class="labelForm" for="passowrd">New Password:</label>
  <input class="inputForm" type="password" name="password" required />
  <input type="hidden"   name="userID" value={{userID}}>
  <button class="btnForm"  type="submit">Cambiar Contraseña</button>
</form>
    </div>
</div>


<script src="https://cdn.jsdelivr.net/npm/sweetalert2@11"></script>

   <script>
        document.addEventListener('DOMContentLoaded', function () {
            const errorMessage = '{{messages.error}}';
            if (errorMessage) {
                Swal.fire({
                    icon: 'error',
                    title: 'Error',
                    text: errorMessage,
                });
            }
        });
    </script>